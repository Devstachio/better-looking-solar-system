{
  "name": "threejs-solar-system",
  "version": "1.0.0",
  "description": "",
  "main": "system.js","dependencies": {
    "gh-pages": "^6.0.0",
    "three": "^0.158.0"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  }
}
